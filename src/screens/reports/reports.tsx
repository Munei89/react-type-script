import React from 'react';
import { Table, Tag } from 'antd';
import { columns, dataSource } from '@constants/reports';

type Props = {};

const Reports: React.FC<Props> = () => {
  return (
    <>
      <div className="reports">Reports</div>
    </>
  );
};

export default Reports;
