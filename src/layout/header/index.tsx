import React from 'react';
import { Layout, Menu } from 'antd';

import './styles.scss';

const { Header } = Layout;

const MainHeader = () => {
  return (
    <Header>
      <h2>QDB</h2>
    </Header>
  );
};

export default MainHeader;
